---------------------------------------- //
--
-- Group By -- SQL StudySpace
--
---------------------------------------- //

/*
There are times we need to group data based on specific values.

SQL offers the GROUP BY clause which can group rows that have similar values into "summary" rows. It returns one row for each group. The interesting part is 
that each group can have an aggregate function applied to it that operates only on the grouped data.

*/

---------------------------- //
-- Group by example
---------------------------- //

/*
Imagine that we have a database with songs and albums, and we want to see how many songs are on each album. We can use a query like this:

*/

